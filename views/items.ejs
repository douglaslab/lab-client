<html>
  <head>
    <%= include partials/head.ejs %>
  </head>
  <body>
    <%= include partials/navbar.ejs %>
    <div class="page-header text-center">
      <h1>List of Inventory Items</h1>
    </div>

    <div class="container">
      <button type="button" class="btn btn-success" id="btnAdd">
        <span class="glyphicon glyphicon-plus"></span> Add Item
      </button>
    </div>
    <div class="container">
      <table class="table table-bordered table-striped">
        <tr>
          <th></th>
          <th>ID</th>
          <th>Properties</th>
          <th></th>
        </tr>
        <% for(var i =0; i < items.length; i++) { %>
        <tr>
          <td><%= i + 1 %></td>
          <td><%= items[i].id %></td>
          <td>
            <% for(var prop in items[i].properties) {
              if(prop !== 'id') { %>
              <%= prop %>: <%= items[i].properties[prop] %><br />
            <% }
            } %>
          </td>
          <td>
            <button type="button" class="btn btn-sm btn-default" data-edit="<%= items[i].id %>">
              <span class="glyphicon glyphicon-pencil"></span> Edit</button>&nbsp;
            <button type="button" class="btn btn-sm btn-danger" data-delete="<%= items[i].id %>">
              <span class="glyphicon glyphicon-trash"></span> Delete</button>
          </td>
        </tr>
        <% } %>
      </table>
    </div>

    <%= include partials/scripts.ejs %>
    <script src="/js/items.js"></script>
  </body>
</html>
